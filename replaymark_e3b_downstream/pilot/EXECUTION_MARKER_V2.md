# Compatibility-fixed pilot execution

The first exploratory run failed before measurement because the Paho VERSION2 callback supplied a `ReasonCode` object. The compatibility fix is now committed without changing the preregistered queue sweep, workload, endpoint, or analysis. This marker triggers execution from the fixed tree.
